# Legacy Code

This folder contains deprecated code pending full removal after replacement systems are validated.

- `PlaceholderEventSource.cs` was superseded by `JsonEventSource` and moved here to keep historical reference while avoiding active production use.
