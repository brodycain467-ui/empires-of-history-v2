# Performance Targets

- O(1) lookup by ID with dictionary indexes
- Mobile tick budget: <16ms
- Memory budgets per entity type must be tracked in generated reports
- Lazy loading for low-frequency domains
- Streaming strategy for map + event archives
- Mobile-first memory constraints
- Future async loading plan through batched background hydration
