# Modding Guide

Empires of History V2 supports data-driven modding without recompilation for leaders, events, governments, religions, technologies, provinces, counties, and cities.

Modders add or override records in JSON databases that match canonical schemas in `data/*/schema.json`.
